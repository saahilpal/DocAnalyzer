<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DocAnalyzer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Prevent the "first splash" white flash by forcing a black window background -->
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <style name="Theme.DocAnalyzer" parent="Base.Theme.DocAnalyzer" />
</resources>